"use client"

import type React from "react"

// Composant de remplacement pour la prévisualisation
export function ThirdwebProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
